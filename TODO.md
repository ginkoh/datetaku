# Todo List

- Stylize stack navigator
- Create a store with useReducer() hook
- Dispatch the accepted/rejected information to the firebase and to the app store.
- Dispatch observations to the firebase/app store
- Use Animated native component to animate the card and the buttons

# Tips
To communicate Parent And Child states in React you should pass a handler function from the Parent to the Child.